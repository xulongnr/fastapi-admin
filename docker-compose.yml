version: '3'
services:
  mysql:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: fastapi-admin
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-p123456"]
      interval: 30s
      timeout: 10s
      retries: 5
  api:
    build: .
    restart: always
    environment:
      DATABASE_URL: mysql://root:123456@mysql:3306/fastapi-admin
      TZ: Asia/Shanghai
    ports:
      - '8000:8000'
    depends_on:
      - mysql
    image: fastapi-admin
    command: uvicorn examples.main:app --port 8000 --host 0.0.0.0
    # command: tail -f /dev/null

    healthcheck:
      test: ["CMD", "/usr/bin/curl", "127.0.0.1:8000"]
      interval: 30s
      timeout: 10s
      retries: 5
